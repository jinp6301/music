<h2><%= @album.name %></h2>

Band: <%= link_to @album.band.name, band_url(@album.band)%>
<br>
Type: <%= @album.location %>

<h3>Tracks</h3>
<ul>
  <% @album.tracks.each do |track| %>
    <li><%= link_to track.name, track_url(track) %></li>
  <% end %>
</ul>

<%= edit_link @album.class.name.downcase %>
<br>
<%= destroy_button @album if @user.admin %>